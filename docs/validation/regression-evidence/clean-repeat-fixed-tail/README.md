# Clean Repeat With Fixed Tail

Captured: 2026-06-30

This physical-iPhone case contains a clean no-pause ten-repeat Work/Recovery block, a fixed cooldown, and continued activity that resolves as `Open / Extra`.

Retained evidence:

- `parity-packet.json`: structured HealthKit/WorkoutKit comparison packet.
- `apple-fitness-1.jpg` and `apple-fitness-2.png`: essential Apple Fitness interval views.

The generalized resolver must expand all repeat rows, map complete contiguous HealthKit activities, exhaust the fixed cooldown, and then preserve the positive residual as `Open / Extra`. A terminal zero-duration pause marker at workout end must not create a dangling-pause failure.

One fresh current-build export remains useful to verify visible/export status agreement; the boundary behavior itself is already regression-tested.
